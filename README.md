# SQL Tutor

This project provides a simple API for learning SQL through lessons, a sandbox database, and practice exercises.

## Setup

```bash
pip install -r requirements.txt
uvicorn src.main:app --reload
```

Use the API endpoints to explore lessons, execute SQL queries, and receive AI hints.
